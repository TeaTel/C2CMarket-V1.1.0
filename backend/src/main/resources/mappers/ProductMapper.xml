<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.backend.mapper.ProductMapper">

    <!-- 商品列表查询 - 支持动态条件、排序、分页 -->
    <select id="selectProductList" resultType="com.campus.backend.entity.Product">
        SELECT * FROM products
        <where>
            <!-- 状态筛选 (默认查在售) -->
            <if test="query.status != null">
                AND status = #{query.status}
            </if>
            <!-- 分类筛选 -->
            <if test="query.categoryId != null">
                AND category_id = #{query.categoryId}
            </if>
            <!-- 关键词搜索 (标题+描述) -->
            <if test="query.keyword != null and query.keyword != ''">
                AND (name LIKE CONCAT('%', #{query.keyword}, '%')
                     OR description LIKE CONCAT('%', #{query.keyword}, '%'))
            </if>
            <!-- 卖家筛选 -->
            <if test="query.sellerId != null">
                AND seller_id = #{query.sellerId}
            </if>
            <!-- 卖家ID列表筛选 -->
            <if test="query.sellerIds != null and query.sellerIds.size() > 0">
                AND seller_id IN
                <foreach collection="query.sellerIds" item="sid" open="(" separator="," close=")">
                    #{sid}
                </foreach>
            </if>
            <!-- 成色筛选 -->
            <if test="query.conditionLevel != null">
                AND condition_level = #{query.conditionLevel}
            </if>
            <!-- 价格区间 -->
            <if test="query.minPrice != null">
                AND price &gt;= #{query.minPrice}
            </if>
            <if test="query.maxPrice != null">
                AND price &lt;= #{query.maxPrice}
            </if>
            <!-- 故事筛选 -->
            <if test="query.hasStory != null">
                AND has_story = #{query.hasStory}
            </if>
            <!-- 标签筛选 -->
            <if test="query.tag != null and query.tag != ''">
                AND FIND_IN_SET(#{query.tag}, tags)
            </if>
            <!-- 校区标签筛选 -->
            <if test="query.campusTag != null and query.campusTag != ''">
                AND campus_tag = #{query.campusTag}
            </if>
        </where>
        <!-- 排序 -->
        <choose>
            <when test="query.sortBy == 'price_asc'">ORDER BY price ASC</when>
            <when test="query.sortBy == 'price_desc'">ORDER BY price DESC</when>
            <when test="query.sortBy == 'view_count'">ORDER BY view_count DESC, created_at DESC</when>
            <otherwise>ORDER BY created_at DESC</otherwise>
        </choose>
        LIMIT #{query.size} OFFSET #{query.offset}
    </select>

    <!-- 商品总数查询 -->
    <select id="selectProductCount" resultType="int">
        SELECT COUNT(*) FROM products
        <where>
            <if test="query.status != null">
                AND status = #{query.status}
            </if>
            <if test="query.categoryId != null">
                AND category_id = #{query.categoryId}
            </if>
            <if test="query.keyword != null and query.keyword != ''">
                AND (name LIKE CONCAT('%', #{query.keyword}, '%')
                     OR description LIKE CONCAT('%', #{query.keyword}, '%'))
            </if>
            <if test="query.sellerId != null">
                AND seller_id = #{query.sellerId}
            </if>
            <if test="query.sellerIds != null and query.sellerIds.size() > 0">
                AND seller_id IN
                <foreach collection="query.sellerIds" item="sid" open="(" separator="," close=")">
                    #{sid}
                </foreach>
            </if>
            <if test="query.conditionLevel != null">
                AND condition_level = #{query.conditionLevel}
            </if>
            <if test="query.minPrice != null">
                AND price &gt;= #{query.minPrice}
            </if>
            <if test="query.maxPrice != null">
                AND price &lt;= #{query.maxPrice}
            </if>
            <if test="query.hasStory != null">
                AND has_story = #{query.hasStory}
            </if>
            <!-- 校区标签筛选 -->
            <if test="query.campusTag != null and query.campusTag != ''">
                AND campus_tag = #{query.campusTag}
            </if>
        </where>
    </select>

    <!-- 更新商品信息 (动态更新) -->
    <update id="update">
        UPDATE products
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="price != null">price = #{price},</if>
            <if test="originalPrice != null">original_price = #{originalPrice},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="conditionLevel != null">condition_level = #{conditionLevel},</if>
            <if test="imageUrls != null">image_urls = #{imageUrls},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
            <if test="location != null">location = #{location},</if>
            <if test="deliveryMethod != null">delivery_method = #{deliveryMethod},</if>
            <if test="storyTitle != null">story_title = #{storyTitle},</if>
            <if test="storyContent != null">story_content = #{storyContent},</if>
            <if test="storyImages != null">story_images = #{storyImages},</if>
            <if test="hasStory != null">has_story = #{hasStory},</if>
            <if test="saleMode != null">sale_mode = #{saleMode},</if>
            <if test="tags != null">tags = #{tags},</if>
            <if test="campusTag != null">campus_tag = #{campusTag},</if>
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

</mapper>
